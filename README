This display the status of a hackerspace in firefox which are supported by this add-on.

The init script is the attempt to speed up the creation of new plugins but there is a better way to do it using the Extension Builder.
